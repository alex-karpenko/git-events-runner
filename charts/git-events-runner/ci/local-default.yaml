image:
  repository: git-events-runner
  tag: local

rbac:
  create: true
  controller:
    secretsNamespaces:
      - default
    jobsNamespaces:
      - default

actionJobServiceAccounts:
  namespaces:
    - default

runtimeConfig:
  action:
    containers:
      cloner:
        image: gitrepo-cloner:local
